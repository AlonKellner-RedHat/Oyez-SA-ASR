# Sideloaded extensions (not on OpenVSX). Downloaded to .devcontainer/vendor/ if missing.
# Format: publisher extension_id version (one per line). Comment lines (#) and empty lines ignored.
sukumo28 wav-preview 2.6.0
VMASPAD video-player 2.0.0
